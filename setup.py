from setuptools import setup, find_packages

setup(
    name='myutils',
    version='1.0.0',
    description='General utils that might be helpful across the projects',
    author='Tomasz Tarczyński',
    author_email='tomek.tarczynski@gmail.com',
    packages=find_packages(),
    install_requires=[],  # Add any dependencies here
)